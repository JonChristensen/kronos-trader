[project]
name = "kt-kronos"
version = "0.1.0"
description = "Kronos financial foundation model wrapper for prediction and ensemble analysis"
requires-python = ">=3.12"
dependencies = [
    "kt-shared",
    "torch>=2.0",
    "pandas>=2.0",
    "numpy>=1.24",
    "huggingface-hub>=0.20",
    "einops>=0.8",
    "safetensors>=0.4",
]
